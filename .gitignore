# Python
.venv/
__pycache__/
*.pyc
 
# dbt
target/
dbt_packages/
logs/
profiles.yml
 
# secrets & environment
.env
*.pem
 
# raw data (too large / not source-controlled)
data/*.csv
data/*.zip
 
# OS
.DS_Store
Thumbs.db

# Jupyter
.ipynb_checkpoints/

# ETL pipeline data (raw source data is not committed to git)
data/raw/*
data/interim/*
data/processed/*
!data/raw/.gitkeep
!data/interim/.gitkeep
!data/processed/.gitkeep

# integration output (regenerate with data/integration/combine.py) — exceeds GitHub file size limit
data/integration/final_combined.csv
